# JIJIE 项目实施清单

## 1. 当前共识

截至 2026-04-13，我们对项目现状的判断已经更新，实施策略也需要同步调整。

- 当前 [data/jijie](D:/work/fenge/data/jijie) 这套整理后的数据集，仍然可以作为现阶段训练与验证的主入口。
- 原始导出库 [fenge_datasets](D:/work/fenge/fenge_datasets) 的主要作用，是做数据审计、补标回流、后续任务化 manifest 重建，而不是立刻完全替代现有数据集。
- 当前效果不理想的主矛盾，不是“之前的数据集整理脚本把大量可用数据弄坏了”，而是“训练任务定义、采样策略、分辨率策略和评估策略不对”。
- 因此，项目改造顺序应调整为：
  1. 保留现有整理数据作为基线继续推进训练改造。
  2. 同步推进原始数据审计和医生补标。
  3. 在补标积累到一定规模后，再重建增强版任务化数据集。

## 2. 审计结论

基于 [audit_summary.json](D:/work/fenge/data/jijie_audit/audit_summary.json:1) 的当前结论：

- 原始扫描样本数：`444`
- 去重后规范样本数：`424`
- 真正无效样本：`2`
- 有具体标注对象的样本：`243`
- 有标签模板但无具体标注对象的样本：`148`
- 连标签模板也基本为空壳的样本：`31`

这说明：

- 数据集不是“整体不可用”。
- 现有整理数据作为第一阶段训练基线是合理的。
- 但数据量仍然偏紧，尤其是少数类和结构类任务，后续补标仍然有价值。

## 3. 总体改造方向

### 3.1 不再把问题定义为单一 14 类分割

第一阶段不再继续推进“14 类统一训练、统一推理、统一评估”的方案，而是改成面向最终量化目标的分任务训练。

### 3.2 数据集策略分成短期和中期两层

短期策略：

- 继续使用 [data/jijie](D:/work/fenge/data/jijie) 作为训练和验证基线。
- 优先改训练方式、采样方式、patch 策略和指标计算方式。

中期策略：

- 继续维护 [data/jijie_audit](D:/work/fenge/data/jijie_audit) 里的审计结果。
- 将 [no_annotation_samples_for_relabel.csv](D:/work/fenge/data/jijie_audit/no_annotation_samples_for_relabel.csv:1) 作为医生补标清单。
- 等补标回流后，再基于原始库重建任务化 manifest 和增强版训练集。

### 3.3 模型结构不是第一优先级

第一阶段先保留当前主干模型体系做基线对照，优先解决以下问题：

- 训练目标不匹配
- 小目标被统一缩放伤害
- 正负样本混用不清
- 稀有类和结构类采样不足
- 评估指标和最终量化目标脱节

## 4. 数据集使用策略

## 4.1 当前训练基线

当前默认使用 [data/jijie](D:/work/fenge/data/jijie)。

使用原则：

- 先不推翻已有整理结果。
- 现有 train/val/test 或后续 group-wise folds，继续围绕这套数据组织。
- 当前训练改造以“最大化现有 243 张已标注样本价值”为核心。

## 4.2 原始库的角色

当前 [fenge_datasets](D:/work/fenge/fenge_datasets) 主要承担三类工作：

- 审计：确认哪些样本有具体标注、哪些没有、哪些需要医生复核。
- 补标：把 `179` 张无具体标注对象的样本整理给医生重新标注。
- 回流：当补标完成后，作为下一版任务化数据集重建来源。

## 4.3 对 zero-mark 样本的处理原则

`groups_but_zero_marks` 不能直接视为可用负样本。

当前建议：

- 第一阶段训练，不把这批样本默认加入负样本训练。
- 只有在医生明确确认“该图对该任务已经穷尽标注、确实没有目标”后，才可进入 `hard negative` 集。
- 在医生未确认前，这批样本只保留为：
  - `inference_only`
  - `manual review`
  - `future relabel`

## 4.4 对 template-only 样本的处理原则

`template_only_no_mark_tables` 当前更应视为“空壳导出”或“待补标样本”。

处理原则：

- 不用于正样本训练。
- 不用于负样本训练。
- 不参与最终评估。
- 直接进入医生补标清单。

## 4.5 何时重建数据集

满足以下任一条件后，再启动增强版数据集重建：

- 医生补回一批高价值样本。
- 我们需要正式落地任务化 folds 和任务化 manifest。
- 当前 [data/jijie](D:/work/fenge/data/jijie) 的组织形式已经无法支撑新的 patch 读取和评估链路。

在此之前，不把“重建数据集”设为主线阻塞项。

## 5. 任务分组

### 5.1 主线 A：线粒体状态分割

标签：

- `1` 严重损伤线粒体
- `2` 中度损伤线粒体
- `3` 健康线粒体
- `4` 自噬线粒体

目标：

- 数量
- 占比
- 面积
- 周长
- 圆度
- Feret 直径
- 长宽比
- 形态因子

特点：

- 小目标多
- 类间极不均衡
- 强依赖高分辨率局部信息

### 5.2 主线 B：线粒体-肌浆网关系

标签：

- `1,2,3,4,5`
- 即 4 类线粒体状态 + `5` 肌浆网

目标：

- 肌浆网面积
- 肌浆网面积占比
- 肌浆网到最近线粒体边界距离

特点：

- 既要保留线粒体细节，又要保留局部空间关系
- 对 patch 尺度要求比线粒体纯分类略大

### 5.3 主线 C：肌节几何结构

核心标签：

- `10` Z 线
- `12` M 线

可选上下文标签：

- `11` T 管
- `13` 心肌侧管

目标：

- 肌节长度
- Z 线长度
- M 线长度
- AsR
- Cur
- `Cur/AsR`
- alpha
- H 参数

特点：

- 依赖大尺度上下文
- 不能被激进缩放
- 更适合整图或大 patch 训练

### 5.4 暂缓标签

下列标签暂不进入第一阶段主训练链路：

- `6` 闰盘
- `7` Z 线样物质堆积
- `8` 脂滴
- `9` 糖原颗粒

原因：

- 样本量偏少
- 与第一阶段核心量化目标关联较弱
- 容易拖累主任务训练稳定性

## 6. 数据处理与 patch 方案

## 6.1 总原则

- 不再统一把图像先缩成 `512x512` 再做训练。
- patch 应面向任务设计，而不是所有目标共用同一套输入尺度。
- 最终预测结果必须能回投到原图坐标。

## 6.2 线粒体任务 patch 方案

建议配置：

- 首选 patch：`640x640`
- 显存紧张时：`512x512`
- 显存允许时：`768x768`
- 训练重叠：`50%`
- 推理 stride：`256-320`

训练采样建议：

- `60-70%` 含目标正样本 patch
- `20-30%` 来自已标注图像中的纯背景 patch
- `0-10%` 来自医生确认后的穷尽负样本图像

这里有一个关键调整：

- 第一阶段不依赖未经确认的 `zero-mark` 图像做负样本。
- 先依靠“已标注图中的背景区域”控制误检，减少伪负样本引入的噪声。

## 6.3 线粒体-肌浆网任务 patch 方案

建议配置：

- patch：`640x640` 或 `768x768`
- 必须保证 patch 中能保留线粒体与肌浆网的相对位置关系
- 对含 `肌浆网` 的 patch 做过采样

重点：

- 该任务不只是分割多一类，而是为后续距离量化服务。
- patch 采样时要优先保留“线粒体与肌浆网共现”的区域。

## 6.4 肌节结构任务输入方案

建议配置：

- 优先整图训练
- 若显存不足，则使用大 patch，如 `1024x1024` 或 `1536x1536`

原则：

- 等效输入边长不低于 `1024`
- 避免把 Z 线、M 线细结构压缩过小
- 可在训练标签生成阶段对极细线结构做适度增粗
- 但最终量化必须回到原始几何中心线

## 6.5 比例尺与角落文字

- 先解析比例尺信息，再决定是否裁掉角落文字。
- 在 `um_per_pixel` 没提取完成前，不应贸然删除比例尺区域。
- 左下角文字若构成非生物学捷径，应从训练输入中遮挡，但不影响比例尺解析。

## 7. 训练方案

结论先写清楚：训练方案需要改，而且需要明显改。

主改进方向不是“换一个更复杂的模型”，而是“把训练组织方式改对”。

## 7.1 通用训练原则

- 不再进行 14 类统一训练。
- 所有切分必须按 `group_id / case_id` 做，不能按 patch 随机切。
- 由于当前有效 group 数有限，开发阶段优先使用 group-wise 交叉验证。
- 训练配置按任务独立管理，不能继续靠一份大而全配置去兼容所有任务。

推荐：

- 开发期：`5-fold group-wise CV`
- 定型后：再保留一组固定 holdout test

## 7.2 第一阶段训练优先级

建议按下面顺序推进：

1. 先把“线粒体状态分割”训稳。
2. 再做“线粒体-肌浆网关系”。
3. 最后做“肌节几何结构”。

原因：

- 线粒体任务与最终量化最直接。
- 线粒体任务样本利用率最高。
- 肌节任务对输入尺度、几何规则和评估链路要求更高，适合放在后面收敛。

## 7.3 线粒体状态分割训练方案

建议标签：

- `1,2,3,4`

建议训练策略：

- 输入：`640x640` patch
- 损失：优先 `CE + Dice`
- 若类不均衡明显拖累少数类召回，再切换到 `Focal + Dice` 或 `Tversky + Dice`
- batch 内强制混合：
  - 含目标 patch
  - 已标注图像中的背景 patch
- 训练轮数参考：`120-180`

模型选择标准：

- 4 类线粒体宏平均 Dice
- 少数类对象级召回率
- 图像级数量误差

比原方案新增的关键点：

- 不再看单纯像素 mIoU 决定成败
- 必须把对象级统计误差纳入主选择标准

## 7.4 线粒体-肌浆网关系训练方案

建议标签：

- `1,2,3,4,5`

建议训练策略：

- 输入：`640x640` 或 `768x768`
- 损失：`CE + Dice`
- 对 `肌浆网` 类加权
- batch 内保证一定比例的“线粒体 + 肌浆网共现 patch”

模型选择标准：

- 肌浆网 Dice / IoU
- 最近边界距离误差
- 图像级面积统计误差

关键提醒：

- 这个任务不是为了追求一个更高的总 mIoU。
- 重点是让后续“距离”和“面积”指标稳定。

## 7.5 肌节几何结构训练方案

建议标签：

- 核心：`10,12`
- 可选上下文：`11,13`

建议训练策略：

- 输入：整图或大 patch
- 损失：`CE + Dice`
- 对细结构类增加权重
- 增强只做轻微旋转和翻转，避免破坏结构方向语义

模型选择标准：

- Z 线 Dice / IoU
- M 线 Dice / IoU
- 下游长度、AsR、Cur 误差

关键提醒：

- 肌节任务不能沿用线粒体任务的小 patch 思路。
- 这个任务更强调几何连续性，而不是像素占比。

## 7.6 Hard Negative 策略

这是训练方案里必须纠正的一点。

规则如下：

- 未确认的 `zero-mark` 样本，不进入训练负样本。
- 第一阶段误检控制，主要依赖“已标注图中的背景区域”。
- 医生确认后的穷尽负样本，单独建白名单，再逐步加入训练。

这样做的原因：

- 可以减少把“漏标图”误当成负样本的风险。
- 对当前数据量有限的项目，更应优先保证标签纯度。

## 7.7 建议的配置组织方式

建议配置目录按任务拆开：

- `configs/mito/`
- `configs/mito_sr/`
- `configs/sarcomere/`

每个配置文件至少显式包含：

- 标签 id
- 输入尺寸
- overlap / stride
- 采样比例
- loss 配方
- 是否启用 confirmed hard negatives
- fold 清单或 split manifest
- 输出目录命名规则

## 8. 推理与指标计算规范

## 8.1 汇报层级

所有结果分三层：

1. patch 级，仅用于调试
2. 图像级，主要量化单元
3. 病例级，最终汇报单元

patch 级指标不能直接作为最终科学结论。

## 8.2 分割指标

每个任务至少汇报：

- Dice
- IoU
- Precision
- Recall
- Per-class 指标

小目标任务额外汇报：

- 对象级 Precision
- 对象级 Recall
- 实例计数误差

## 8.3 线粒体量化规则

在线粒体任务中，必须先把 patch 预测回投到原图，再做连通域或实例提取。

之后计算：

- 各类数量
- 各类比例
- 单实例面积
- 单实例周长
- 面积占比
- 圆度
- Feret
- 长宽比
- 形态因子

聚合方式：

- 图像级输出均值、中位数、总数
- 病例级对图像级统计再聚合
- 保留逐对象明细表，保证可追溯

## 8.4 肌浆网量化规则

在原图坐标下计算：

- 肌浆网总面积
- 肌浆网面积占比
- 肌浆网实例平均面积
- 每个肌浆网实例到最近线粒体实例的边界到边界最短距离

距离定义：

- 使用边界到边界的最短欧氏距离
- 不使用质心到质心距离

## 8.5 肌节几何量化规则

只有满足几何有效性规则的连续结构，才进入肌节量化。

有效性规则示例：

- 至少 `4` 个连续肌节
- Z 线与 M 线配对关系成立
- 几何拟合稳定

输出：

- Z 线长度
- M 线长度
- 肌节长度
- AsR
- Cur
- `Cur/AsR`
- alpha
- H 参数

所有被排除的序列，都必须记录排除原因。

## 8.6 物理单位

- 能解析比例尺时，最终结果以物理单位输出。
- 比例尺缺失时，可保留像素空间调试结果，但不进入最终生物学汇总表。

## 9. 实施顺序

### 阶段 1：先改训练，不先推翻数据集

- 继续使用 [data/jijie](D:/work/fenge/data/jijie)
- 拆出三条任务线
- 重构配置、采样和 patch 策略
- 跑出新的 group-wise baseline

### 阶段 2：补齐推理与量化闭环

- 做滑窗推理与回投原图
- 建立对象级和图像级量化
- 把评估指标从像素级提升到任务指标级

### 阶段 3：并行推进补标

- 使用 [no_annotation_samples_for_relabel.csv](D:/work/fenge/data/jijie_audit/no_annotation_samples_for_relabel.csv:1)
- 组织医生回标
- 逐步形成 confirmed hard negative 和新增正样本

### 阶段 4：补标后重建增强版数据集

- 基于原始库和补标结果重建任务化 manifest
- 更新训练 folds
- 重跑增强版实验

## 10. 当前最优先动作

按优先级排序：

1. 更新训练代码，使其支持任务化配置。
2. 将线粒体任务从 14 类统一训练中拆出来。
3. 引入高分辨率 patch 训练，不再统一缩成 `512x512`。
4. 把验证标准从单纯像素指标改成对象级与图像级指标。
5. 并行维护补标清单，但不把补标完成作为当前训练改造的前置阻塞。

## 11. 一句话结论

这份方案的最新版本应明确反映以下判断：

- 数据集基线没有根本性方向错误。
- 当前阶段继续使用 [data/jijie](D:/work/fenge/data/jijie) 是对的。
- 训练方案必须改，而且改造重点应放在任务拆分、patch 分辨率、采样逻辑、hard negative 边界和量化评估闭环上。
